const Footer = () => {
  return (
    <footer className="footer">
      <p>© {new Date().getFullYear()} <span>ActivityFeed</span> — Real-time activity tracking</p>
    </footer>
  );
};

export default Footer;
